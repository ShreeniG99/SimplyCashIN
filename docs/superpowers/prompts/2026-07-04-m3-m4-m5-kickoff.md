# SimplyCashIN — M3+M4+M5 Build Sprint (single session, hard deadline 6:00 PM)

## Role & mission
You are the sole implementer finishing SimplyCashIN — a multi-agent receivables-collection
system for Indian MSMEs. M1 (decision loop + API) and M2 (ingestion, scheduler, Redis queue)
are DONE, audited, and green. Your job: implement **M3 (channels & dispatch), M4 (auth &
multi-tenancy), M5 (realtime HITL + frontend wiring)** — production-shaped, tested, demoable —
by 6:00 PM today. Full-product test pass by 7:00 PM. Work autonomously; do not ask questions
unless truly blocked on a secret only I can provide.

## Ground truth (read ONCE, extract notes, never re-read)
1. `docs/superpowers/specs/2026-06-19-simplycashin-vertical-slice-design.md` — architecture,
   milestone table (M3/M4/M5 rows), error-handling rules, non-negotiables.
2. `backend/README.md` — run commands.
Do NOT re-audit M1/M2. Trust the state summary below; verify only by running the suite once.

## Current state (verified 2026-07-04, all evidence in git log)
- 60+ tests green (`cd backend && USE_STUB_LLM=1 .venv/Scripts/python -m pytest tests`).
  `test_integration_claude.py` is opt-in (real API spend) — skip unless ANTHROPIC_API_KEY set.
- Stack: FastAPI + async SQLAlchemy + Postgres/pgvector + Redis via
  `backend/docker-compose.yml`; Alembic migrations 0001–0003 applied; seed via
  `python scripts/seed_dev.py` (owner `ramesh`, 5 buyers, demo date pinned 2026-05-18).
- Seams you MUST build on (do not reinvent):
  - `app/channels/base.py` — `Channel` protocol + `DispatchResult`; `SimulatedChannel` is the
    no-creds default. Orchestrator already escalates on `DispatchResult.ok == False`.
  - `app/llm/base.py` — `LLM` protocol; `deps.get_llm()` picks stub vs `AnthropicClient`
    (`claude-opus-4-8`); all SDK errors already map to `LLMError` ⇒ escalate.
  - `app/queue/redis_queue.py` — owner-namespaced zset `scin:urgency:{owner_id}`, client has
    5s timeouts + 3-retry backoff; re-enqueue is idempotent (member = buyer/invoice/due_date).
  - `app/scheduler/daily.py` — `DailyOverdueTrigger` + `build_scheduler()`, started in app
    lifespan only when `ENABLE_SCHEDULER=1`.
  - `memory_embedding.buyer_id` is the vector namespace column (add `owner_id` in M4).
  - `InvoiceRepo` derives `days_overdue` at read from `due_date` (pass `today=`).
  - `frontend/src/lib/api.js` already matches every backend contract incl. M2 endpoints.

## Scope (in priority order — a working demo of all three beats polish on any one)

### M3 — Channels & dispatch (spec row: "Celery+Redis async dispatch with retry, Twilio
WhatsApp/SMS, aiosmtplib email, inbound reply webhooks")
- `TwilioChannel` (WhatsApp+SMS) and `EmailChannel` (aiosmtplib) implementing `Channel`.
  Credentials from env (`TWILIO_*`, `SMTP_*`); **without creds, factory returns
  SimulatedChannel** — tests and demo never need real creds. Unit-test with faked transports.
- Celery worker (`app/dispatch/`) consuming the urgency queue: task takes buyer/invoice,
  runs the cycle, dispatches via the channel factory, retries transient send failures
  (max 3, backoff), records failure ⇒ escalation on exhaustion.
- **MANDATED DECISION (carry-over from M2 audit):** revisit `ENABLE_SCHEDULER`. Split per
  spec: APScheduler/beat DETECTS overdue → enqueues; Celery DISPATCHES. Do not duplicate the
  job in both. Document the decision in README and set the default accordingly.
- Inbound reply webhook `POST /webhooks/twilio` → appends buyer `ConversationTurn` (signature
  check stubbed behind env flag).

### M4 — Auth & multi-tenancy (spec row: "Supabase Auth + JWT, per-owner data + vector-
namespace sandboxing (Postgres RLS)")
- JWT verification dependency (Supabase-issued HS256, secret from `SUPABASE_JWT_SECRET`);
  `owner_id` comes from the token claim, replacing the hard-coded `OWNER_ID` in deps/routes.
  **Dev fallback:** when `SUPABASE_JWT_SECRET` unset, resolve to `ramesh` so M1–M3 tests and
  the demo run unchanged.
- Migration 0004: add `owner_id` to `memory_embedding` (+ backfill from buyer), and RLS
  policies on owner-scoped tables keyed to `current_setting('app.owner_id')`. Repos filter by
  owner everywhere a table has one.
- Tests: two owners seeded; owner A's token cannot read owner B's buyers/queue/memory.

### M5 — Realtime HITL + frontend (spec row: "WebSocket HITL gateway; real React app")
- `WS /ws/escalations`: pushes new escalations live; approve/edit/override over the socket
  reuses the existing resolve logic (approve ⇒ dispatch + memory — already implemented).
- Frontend: `cd frontend && npm install && npm run dev`; wire the WebSocket into the
  Escalation screen; confirm every screen renders against the live API (VITE_API_BASE).
- Demo script in README: seed → run-cycle (ACT for rajan, ESCALATE for anand) → approve in
  the UI over WS → CSV ingest → trigger → queue drain via Celery.

## Non-negotiables (grep-verified in M1/M2 — do not regress)
ACT/ESCALATE stays plain Python over PolicyCheck results (never an LLM call); nothing is sent
to a buyer on a failed check without owner approval; tracer failures swallowed; LLM failure /
schema-invalid plan / refusal ⇒ escalate; money is integer paise (`_paise`), ₹-formatted only
at the API edge; keep WFDATA shapes; smallest correct change, match existing style.

## Method (calibrated for you)
- Build the FULL task list up front (one task per bullet above), then execute without pausing
  to re-plan. When you have enough information to act, act.
- TDD per task: failing test → smallest fix → green → `git commit` (repo root is the project;
  commit per task, imperative messages).
- Don't add features, refactors, or abstractions beyond the task. No hypothetical error
  handling; validate at system boundaries only.
- Before reporting progress, audit each claim against a tool result from this session; if
  unverified, say so.
- Parallelize: independent reads/greps in one batch; long installs/suites in background while
  you write the next test.
- Timebox: M3 by ~3:30, M4 by ~4:45, M5 by ~5:45. If a task overruns, stub it behind the
  existing seam, add it to a CUT-LIST section in your final report, and move on. 6:00–7:00 is
  reserved for the full-product pass: whole suite green + every endpoint/WS/UI flow exercised
  against the running stack + demo script executed end-to-end.

## Environment facts (save re-discovery)
- Repo: `C:\Dev\SimplyCashIN-dev-test\SimplyCashIN-dev-test` (git root). Backend venv:
  `backend/.venv` → run `cd backend && .venv/Scripts/python -m ...` (never bare `.venv` from
  repo root). Python 3.12.
- Docker Desktop is UNSTABLE on this machine: if db tests suddenly skip/hang mid-suite or you
  see `WinError 64`/`dockerDesktopLinuxEngine` pipe errors, kill ALL docker* processes, start
  Docker Desktop ONCE, wait for `docker info`, `docker compose up -d` from `backend/`.
  A standalone `redis` container may own port 6379 instead of `backend-redis-1` — either works.
- Tests: `USE_STUB_LLM=1 .venv/Scripts/python -m pytest tests` (db tests self-provision
  `scin_test` + pgvector). API: `uvicorn app.api.app:app --port 8123`. New deps go in
  `pyproject.toml` AND `requirements.txt`.

## Deliverable (final message)
1. Task list with DONE/CUT status and commit hash each.
2. Scheduler/Celery split decision and why.
3. Full-suite result + functional pass log (endpoint → observed result, incl. WS + UI).
4. Exact demo-run commands.
